# Copy this file to .env and fill values before running the project
# Minimal env vars used by the project
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost
# DATABASE: default is sqlite (db.sqlite3). To use other DBs, set DATABASE_URL accordingly.
# DATABASE_URL=postgres://user:pass@localhost:5432/dbname

# Pesapal configuration (set these from your Pesapal merchant dashboard)
# Use the production values below for live environment or sandbox URLs for testing.
# Do NOT commit your real credentials into the repo; store them in your environment or a secrets manager.
PESAPAL_CONSUMER_KEY=
PESAPAL_CONSUMER_SECRET=
# Optional: override endpoints if you want sandbox URLs
# Sandbox token URL: https://cybqa.pesapal.com/pesapalv3/api/Auth/RequestToken
# Live token URL: https://pay.pesapal.com/v3/api/Auth/RequestToken
PESAPAL_TOKEN_URL=https://pay.pesapal.com/v3/api/Auth/RequestToken
PESAPAL_SUBMIT_URL=https://pay.pesapal.com/v3/api/Transactions/SubmitOrderRequest
PESAPAL_REGISTER_IPN_URL=https://pay.pesapal.com/v3/api/URLSetup/RegisterIPN
PESAPAL_CURRENCY=TZS
