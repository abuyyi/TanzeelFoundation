# Runtime dependencies for Tanzeel Foundation (Python 3.9+).
# Pinned to the versions verified working locally.
# Note: MySQL is accessed via PyMySQL (pure-Python) + cryptography, so the
# compiled `mysqlclient` package is intentionally NOT required.

Django==4.2.23
PyMySQL==1.1.2
cryptography==49.0.0
Pillow==11.3.0
whitenoise==6.11.0
python-dotenv==1.2.1
requests==2.32.5
python-slugify==8.0.4

# Admin UI theming
django-admin-interface==0.32.0
django-colorfield==0.14.0
django-flat-theme==1.1.4

# Transitive / supporting libraries (pinned for reproducibility)
asgiref==3.9.1
sqlparse==0.5.3
tzdata==2025.2
certifi==2025.11.12
charset-normalizer==3.4.4
idna==3.11
urllib3==2.6.2
cffi==2.0.0
pycparser==2.23
text-unidecode==1.3
typing_extensions==4.15.0
packaging==26.0
